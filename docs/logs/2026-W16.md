# 2026-W16 周日志

时间范围：2026-04-13 ~ 2026-04-19

## 本周目标

- 完成 Windows -> Linux 的项目重整
- 将数据、权重、输出迁移到可跨系统共享盘
- 将环境从仓库内 `venv` 迁移到 Miniconda
- 收敛主线代码并归档历史探索脚本

## 本周完成

- 完成目录外置与软连接映射：
  - `res -> /mnt/data/aigc_data/datasets/res`
  - `output -> /mnt/data/aigc_data/outputs/output`
  - `adapter_strategy_v1/checkpoints -> /mnt/data/aigc_data/checkpoints/adapter_strategy_v1/checkpoints`
  - `mat -> /mnt/data/aigc_data/refs/mat`
- 新增环境文件 `environment.yml`，创建 `aigc-m-py311` conda 环境并完成关键依赖导入验证。
- 清理仓库内 `venv/`，减少仓库体积与同步负担。
- 完成主线与历史代码分离：
  - `src/` 保留当前主流程脚本
  - 历史代码迁移到 `archive_legacy/`
- 修复主线脚本中的 Windows 绝对路径，改为项目相对路径。
- `adapter_strategy_v1/generate_adapter.py` 改为优先使用本地微调模型目录 `res/musicgen_finetuned`，避免误用远程未微调模型。

## 关键问题与修复

- 问题：多处脚本仍使用 `D:\\pyprojects\\aigc-m`，Linux 下报 `FileNotFoundError`。
  - 修复：统一改为 `Path(__file__).resolve().parents[1]` 推导项目根路径。
- 问题：在线拉取 `facebook/musicgen-small` 不稳定，推理容易卡住。
  - 修复：生成脚本默认改为本地微调模型加载。
- 问题：输出目录在外部挂载盘，受沙箱权限影响可能写入失败。
  - 修复：验证运行时使用可访问外部挂载盘的执行权限。

## 产出文件/目录变化

- 新增：
  - `scripts/reorganize_linux.sh`
  - `environment.yml`
  - `archive_legacy/README.md`
  - `docs/`（文档分层目录）
- 迁移：
  - 历史脚本与实验脚本迁入 `archive_legacy/`

## 下周计划

- 与同学对接真实 EEG 连续特征格式（pt/npy、字段、维度、标准化方式）。
- 将 `train_adapter.py` 从 one-hot fallback 迁移为“必须真实 EEG 输入”。
- 建立 `experiments/` 的首批实验台账（one-hot baseline vs 连续特征）。
